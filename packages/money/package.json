{
  "name": "@ai-gateway/money",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "金额计算（整数厘，先乘后除一次舍入）：费用/hold/套餐额度，gateway 与 worker 共用",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "decimal.js": "^10.6.0"
  }
}
