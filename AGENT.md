# AGENT.md —— 工程规范

> 本文档是本仓库的**代码规范**：架构分层、各层写法、资金域专项纪律与质量门禁。
> 与代码冲突时：先查本文规则是否被违反，是则修代码；规则过时则先改本文再动代码。
> 全程中文注释；提交信息用英文 Conventional Commits（见 §7）。
> **代码是业务逻辑的唯一标准**——docs/ 下的文档仅作导读，可能与实现不同步，与代码冲突时一律以代码为准。

---

## 0. 铁律（每条都是硬约束，违反 = 返工）

1. **单向分层**：最终结构为 `routes → application → domain`，存在真实边界时使用
   `application → ports ← adapters → db`；
2. **职责位置唯一**：业务规则只住 domain（纯函数），事务只住 application（迁移前为 service），
   SQL 只住 `adapters/postgres`（迁移前为 repository），表定义只住 `db`。
3. **零写死**：一切可变值（币种、refType、失败策略参数、开关、阈值）必须装配注入且**必填**，
   不藏全局默认；同一真相只定义一次（常量单一来源，放最底层被依赖的包）。
4. **未实现 = bug**：不许「接口先行 / TODO 将来实现 / 占位」。写了接口必须有实现与调用方；
   推迟实现的功能在文档里显式挂「待办」并说明后果。
5. **一动词一文件**：一个用例/动词/规则一个文件；文件超 ~150 行先问自己是不是装了两件事。
   禁止 class 做依赖捕获（用 `createXxx(env)` 工厂闭包——Postgres repository adapter 除外，见 §4）。
6. **改资金逻辑先读懂再动手**：先读 `packages/wallet`、`packages/service` 的现有实现
   （代码是行为语义的唯一标准），基于语义重写而不是复制粘贴。发现现有逻辑错误要修并在 PR 里说明。
7. **行为等价验证**：重构类改动必须有测试兜底且全绿才算完成（见 §5 四门）。
8. **单一形态，不留兼容层**：仓库只保留一套实现；不写兼容旧代码的逻辑——
   旧路径别名、双轨字段、参数双收一律不做，发现即删。
   大体量删除（整包 / 整应用）先开 issue 列清单等维护者确认。
9. **配置不是包，发布不等于 workspace**：TypeScript / Vitest / Oxlint / Oxfmt / Turbo 配置放根目录，
   不建 `tooling` workspace 包；所有 workspace 默认私有，只有经过明确批准且发布闭包完整的包才能独立发布。
10. **目标态按业务能力聚合**：最终采用 `domain ← application`，按真实边界增加 `ports ← adapters`；
    迁移前代码继续遵守现有 `service/domain/repository` 纪律。一个能力必须原子切换并删除旧位置，禁止长期双轨。
11. **边界必须可执行**：package、协议和未来插件边界不能只靠目录命名；显式 `exports`、依赖白名单、
    无环检查、架构测试和发布闭包检查必须在 CI 中执行。没有真实边界的浅包必须合并，不保留永久编制。
12. **数据面与观察面分离**（`ai` 硬约束）：上游响应逐块透传 C 端，不缓冲、不改写、不收完再转发；
    触碰「不改写」的仅有透传例外清单三种情形（重构文档 §3.6）：跨协议最小必要转换（含错误体）、
    响应侧 model 字段替换（可配置开关）、错误出站三层（结构翻译成 OpenAI 信封、内容脱敏后保留
    原文、细节只进日志关联 requestId）。计费取证、审计、trace、渠道健康（熔断/死凭据）一律经
    `onEvent` 监听面旁路消费，不进热路径。`ai` 不持有跨请求运维状态；
    观察 tap 丢失不得造成资损——兜底在 billing 状态机与对账（§6），禁止在热路径同步结算换确定性。
13. **方案先行**：任何功能实现与重构，先按 §9 工作流产出方案文档再动代码；文档未定稿不写实现。
    复制旧代码的前提是**逐文件审计无可挑剔**（四条标准见 §9.1 步骤 2），审计发现记录在案并升级裁决。
    实现中发现文档有误，先改文档再改代码——文档与代码同步演进，禁止口头漂移。
14. **测试目录约定**：包内测试统一放包根的 `__test__/` 目录，目录内**平铺**——
    一律不建子目录（unit / contract / protocol 等分组不做），所有测试文件同级；
    vitest include 固定为 `__test__/*.test.ts`，测试内相对导入 `../src/`。
    需要真实凭证的上游集成测试以文件名区分（`*.real.test.ts`），默认门禁按文件名排除，
    单独脚本显式运行。

---

## 注意老项目代码仓库：/Users/wrr/work/ai-getway
## 总迁移方针文档：/Users/wrr/work/TokenLens-v2/docs/project-structure-refactoring.md


## 9. 方案先行：文档工作流（所有功能实现与重构的强制前置）

> 本节是「方案文档怎么一步一步写出来」的标准流程，由 ai 包重构实践沉淀；后面所有功能实现与重构按此走。

### 9.1 七步流程（每步有产出物，顺序不可跳）

1. **停代码，先设计**。产出设计基线三件：
   - 外部契约：API 形态（参数平铺不嵌套、结果判别联合、可选参数全部有缺省）、事件时序契约；
   - 内部问题域：处理什么 / **明确不处理什么**（不处理的写清归属哪个消费方，不留白）；
   - 并发与性能预算：数字化硬约束（每帧同步 μs 级、每连接常数内存、全局单 timer、回调无 IO）。
   设计讨论中的每个方向性裁决（如错误归一、机制/策略分离）标注「用户裁决」落档。
2. **旧实现全量审计**（有旧代码时）。逐文件过四条标准：
   ① 正确性（边界、错误路径、静默吞错）；② 契约符合（零跨请求状态、无策略数据、无隐藏默认）；
   ③ 实现质量（职责单一、无重复、热路径预算）；④ 依赖方向干净。
   产出三个清单：**真 bug**（编号 B#、位置、级别——计费/数据丢失优先）、**重复代码**（D#、提取计划）、
   **契约缺口**（演进决策）。诚实区分「实测确认」与「待审计」；可以并行委派审计，但裁决必须基于证据。
3. **逐模块裁决**。复制 / 复制+微修 / 重构 / 重写 / 不移植，逐文件列表并附审计状态列。
   **复制的前提是审计无可挑剔**；审计发现升级裁决并回写表格。
4. **拆分决策**。目标目录结构、共享件提取（引用 D#）、契约演进（引用缺口）、并发重构（引用预算）——
   每条决策必须引用审计证据，禁止凭感觉。
5. **测试计划先行**。旧测试 = 行为规格（迁移矩阵：移植/改写/删除+理由）；**每个真 bug 一个回归用例**；
   按设计新增门禁（延迟、内存上界、词表封闭性、兜底可观测）。
6. **分阶段实施**。每阶段独立提交 + 四门验证（typecheck/lint/test/build）全绿；阶段边界可独立回滚；
   提交信息引用文档节号（如 `docs(ai): … §3.2`）。
7. **收口核销**。行为对照清单逐项核销（可观察行为逐条列出）；文档状态推进
   （草稿 → 审计中 → 定稿 → 实施中 → 已完成 → 已核销）。

### 9.2 文档体系（复杂度决定文档数量，不强行合一）

| 复杂度 | 文档组合 |
|---|---|
| 小功能 | 单个 `IMPLEMENTATION.md`（设计 + 测试合一） |
| 包级 / 跨包重构 | `DESIGN.md`（设计基线：外部契约 / 问题域 / 并发预算——定稿后少改）+ `IMPLEMENTATION.md`（施工图：审计 / 裁决 / 拆分 / 测试计划 / 实施顺序）+ **每个迁移单元一份 `MIGRATION.md`** |
| 单项方向性决策 | ADR（`docs/adr/NNNN-kebab-title.md`，编号递增、只进不出、被推翻标记 Superseded） |

位置约定：包级文档放包根或包内 `docs/`；跨包放仓库 `docs/`；ADR 统一 `docs/adr/`。
文档之间用相对链接互引，单一事实源原则同样适用于文档（同一决策只在一处定义，他处引用）。

### 9.3 迁移文档模板（MIGRATION.md）

```markdown
# <模块/垂直用例> 迁移文档
> 状态：草稿 | 审计中 | 定稿 | 实施中 | 已完成 | 已核销
> 迁移单元：<一个可观察的业务行为（垂直用例），不是"一个包">
> 旧实现：<旧仓路径 + 规模（文件数/行数）+ 测试数>
> 目标位置：<新仓路径>
> 关联：DESIGN.md §x / IMPLEMENTATION.md §x / ADR-NNNN

## 1. 行为规格基线
- 旧测试清单（文件 → 用例数）——行为等价的判定标准
- 显式列出**删除**的用例及理由（机制已裁决移除 ≠ 功能缺失，须注明裁决出处）

## 2. 审计结论（引用 IMPLEMENTATION.md，不重复抄写）
- 影响本单元的真 bug（B# 清单与修复决策）
- 涉及的重复提取（D#）与契约演进

## 3. 逐模块裁决表
| 文件 | 裁决 | 审计状态 | 动作 |

## 4. API 对照
| 旧签名 | 新签名 | 变化理由 |

## 5. 测试迁移矩阵
| 旧测试 | 新去处 | 动作（移植 / 改写 / 删除+理由） |

## 6. 回滚方案
- 每阶段提交独立可 revert；旧仓在切换验证前只读不新增；DDL 与代码迁移分离先行

## 7. 验收（全部满足才算完成）
- 四门全绿
- 行为对照清单逐项核销（逐条列出可观察行为）
- 全部 bug 回归用例通过
```

### 9.4 纪律

- 未有文档不动代码（铁律 13）；文档未定稿不写实现。
- 文档与代码同步演进：实现推翻设计时，同一提交内先改文档。
- 「复制」永远指审计通过后复制；审计标准与流程见步骤 2，不可省略。
- 每个实施提交引用其遵循的文档节号，保证可追溯。


